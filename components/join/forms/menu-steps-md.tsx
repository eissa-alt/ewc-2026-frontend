import React from 'react';
import { Translate } from '~/i18n';
import { CircularProgressbar } from 'react-circular-progressbar';
import 'react-circular-progressbar/dist/styles.css';

/******************************************************************
 *
 *  THIS COMPONENT WAS GENERATED BY NEXTCRAZY-CLI
 *
 ******************************************************************/

type MenuStepsMdProps = {
   steps: any;
   step: string | undefined;
};

const MenuStepsMd = ({ steps, step }: MenuStepsMdProps) => {
   const currentStep = steps?.indexOf(step) + 1;
   const currentStepText = steps[currentStep - 1];
   const nextStepText = steps[currentStep];
   return (
      <React.Fragment>
         <div className="row pb-5">
            <div className="col-12">
               <div className="mt-5 rounded-lg border bg-white p-5 shadow-[0px_7px_29px_0px_rgba(100,100,111,0.2)]">
                  <div className="flex space-x-3 rtl:space-x-reverse sm:space-x-5">
                     <div className="my-auto">
                        <div className="h-16 w-16 sm:h-20 sm:w-20">
                           <CircularProgressbar
                              strokeWidth={10}
                              className="progress-radial"
                              styles={{
                                 path: {
                                    stroke: '#4E442D',
                                 },
                                 // Customize the text
                                 text: {
                                    // Text color
                                    fill: '#4E442D',
                                    // Text size
                                    // fontSize: '16px',
                                 },
                              }}
                              //todo: double check
                              value={(100 / steps.length) * currentStep}
                              maxValue={100}
                              text={`${steps.length} / ${currentStep}`}
                           />
                        </div>
                     </div>
                     <div className="my-auto">
                        <div className="text-lg font-bold sm:text-xl">
                           {<Translate id={`web:${currentStepText}`} />}
                        </div>
                        {nextStepText && (
                           <div className="text-sm text-black sm:text-base">
                              <Translate id={`web:next_step`} />{' '}
                              {<Translate id={`web:${nextStepText}`} />}
                           </div>
                        )}
                     </div>
                  </div>
               </div>
            </div>
         </div>
      </React.Fragment>
   );
};

export default MenuStepsMd;
